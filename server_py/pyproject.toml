[project]
name = "lab-db"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0.0",
    "flask-cors>=4.0.0",
    "requests>=2.31.0",
]

[tool.uv]
# 사내 PyPI 미러 주소로 변경하세요
index-url = "https://내부레포주소/simple/"

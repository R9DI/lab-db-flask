{
  "name": "lab-db",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm run server\" \"pnpm run client\"",
    "server": "python -m server_py.main",
    "client": "cd client && pnpm run dev"
  },
  "dependencies": {
    "concurrently": "^9.0.0"
  }
}
